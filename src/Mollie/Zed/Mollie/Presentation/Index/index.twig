{% extends '@Gui/Layout/layout.twig' %}

{% set title = 'mollie.title.payment_methods' | trans %}
{% block content %}

    {% set filterButtonText = 'mollie.button.filter_all' %}
    {% set filterButtonValue = 0 %}
    {% if not showOnlyEnabledPaymentMethods %}
        {% set filterButtonText = 'mollie.button.filter_activated' %}
        {% set filterButtonValue = 1 %}
    {% endif %}
    <div class="col-lg-12">
        <div class="ibox float-e-margins">
            <div class="ibox-title clearfix">
                <h2>
                    {{ 'mollie.profile.information' | trans }}
                    Profile Information
                </h2>
            </div>
            <div class="ibox-content">
                <h4>
                   {{ 'mollie.profile.id' | trans}} {{ profile.id }}
                </h4>
                <h4>
                    {{ 'mollie.profile.status' | trans}} {{ profile.status }}
                </h4>
            </div>
        </div>
    </div>

    <div>
        <a class="btn btn-primary"
           href="/mollie/payment-methods/clear-cache"
        >
            {{ 'mollie.button.clear_cache' | trans }}
        </a>
        <a class="btn btn-primary"
           href="/mollie/payment-methods?showOnlyEnabledPaymentMethods={{ filterButtonValue }}"
        >
            {{ filterButtonText | trans }}
        </a>
    </div>
    {{ mollieTable | raw }}
{% endblock %}
